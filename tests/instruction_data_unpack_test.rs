#[cfg(test)]
mod tests {
    use nft_staking_contract::instruction::NFTStakingContractInstruction;

    #[test]
    fn instruction_data_unpack_test() {
        let mut packed_data = [0, 5, 128, 81, 1, 0, 0, 0, 0, 0, 60, 0, 0, 0, 0, 0, 0, 0];
    }
}
